"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const createFlagObject_1 = require("./createFlagObject");
const isArgument_1 = require("../types/isArgument");
const isFlag_1 = require("../types/isFlag");
/**
 * Creates a {@link RichArgv} object from command parts.
 * @param parts Command parts.
 */
function createRichArgv(parts, options) {
    const args = parts.filter(isArgument_1.isArgument);
    const flags = createFlagObject_1.createFlagObject(parts.filter(isFlag_1.isFlag), options);
    return { args, flags };
}
exports.createRichArgv = createRichArgv;
