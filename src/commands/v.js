const { Client, Intents } = require('discord.js');
const { Player } = require('discord-player');

module.exports = {
  name: "play",
  description: "Воспроизведение музыки из Яндекс.Музыки",

  async run(message, { args }) { // args теперь передаются как часть объекта
    
  }
};
